services:
  web:
    build: .
    working_dir: /app
    volumes:
      - .:/app
    ports:
      - "8888:8000"
    environment:
      PYTHONUNBUFFERED: "1"
    command: python manage.py runserver 0.0.0.0:8000
